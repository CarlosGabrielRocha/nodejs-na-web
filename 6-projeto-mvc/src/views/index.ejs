<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Início | Blog Express</title>
  <link rel="stylesheet" href="/assets/style.css">
</head>

<body>
  <!-- =: Para devolver apenas o html seguro
       -: Normalmente usamos junto ao include para evitar escapar o html duas vezes, visto que normalmente no próprio arquivo a ser incluído, utilizamos o = nas expressões. (Consulte documentação para mais detalhes)
  -->
  <%- include('partials/header')  %>

  <div class="container">
    <h1>Posts Recentes</h1>
    <ul class="posts">
      <% posts.forEach(post => { %>
        <li>
          <article class="post">
            <a href="/posts/<%= post.id %>" class="post-title">
              <h2><%= post.title %></h2>
            </a>
          </article>
        </li>
      <% }); %>
      </ul>
  </div>

  <%- include('partials/footer')  %>
</body>

</html>